# Scanned PDFs: opt in to sending documents to Firecrawl Parse for OCR.
ANYDOC_OCR=off
FIRECRAWL_API_KEY=

# Optional stable Console API key and port. Otherwise generated at startup.
TW_API_KEY=
PORT=0
